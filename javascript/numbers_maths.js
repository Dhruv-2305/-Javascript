// in this file we discovered the numbers to be converted tostring, toprecision, toexponential, tofixed in browsers console 
// please do check at your own 

//example
const balance = new Number(200)
//console.log(balance)

// it will create the Number 200 strictly a number 
// we can assign them tostring, to precision, toexponential,  tofixed.

//+++++++ maths ++++++++

console.log(Math.floor(Math.random()));

const min =10 
const max = 11

console.log((Math.random()*(max - min +1))+ min);

//math.random() gives the value between 0 and 1 .

//math.floor is function used to have precise round off value.
